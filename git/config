[core]
  filemode = true
  excludesfile = ~/.dotfiles/git/excludes

[user]
  name = Lukas Schauer
  email = lukas@schauer.so

[branch]
  autosetupmerge = true
  autosetuprebase = always

[color]
  ui = auto

[push]
  default = simple

[filter "lfs"]
  clean = git-lfs clean %f
  smudge = git-lfs smudge %f
  required = true

[diff]
  ignoreSubmodules = untracked

[github]
  user = lukas2511
